# === Sentinel Configuration ===

# EVM RPC (Base Sepolia or Sepolia)
RPC_URL=https://sepolia.base.org
CHAIN_ID=84532

# Deployer / Operator (NOT the AI agent — used for settlement only)
OPERATOR_PRIVATE_KEY=0x...

# Smart contract addresses (filled after deployment)
SENTINEL_WALLET_ADDRESS=0x...
POLICY_GUARD_ADDRESS=0x...

# ENS
AGENT_ENS_NAME=sentinel-agent.eth

# Session defaults
DEFAULT_SESSION_DEPOSIT_USDC=1000
MAX_TRADE_PERCENT=2
MAX_SLIPPAGE_PERCENT=0.5

# Logging
LOG_LEVEL=debug
