# === Sentinel Configuration ===

# EVM RPC (Base Sepolia or local Anvil)
# For local: start.sh sets this automatically.
# For testnet: use a public RPC like https://sepolia.base.org
#   or a provider URL (Alchemy/Infura — requires API key from them, see below).
RPC_URL=https://sepolia.base.org
CHAIN_ID=84532

# Deployer / Operator (NOT the AI agent — used for settlement only)
OPERATOR_PRIVATE_KEY=0x...

# Smart contract addresses (filled after deployment)
SENTINEL_WALLET_ADDRESS=0x...
POLICY_GUARD_ADDRESS=0x...

# ERC-4337 EntryPoint v0.7 (same on all chains — rarely needs changing)
ENTRYPOINT_ADDRESS=0x0000000071727De22E5E9d8BAf0edAc6f37da032

# Block explorer API key (for contract verification on BaseScan — optional)
ETHERSCAN_API_KEY=

# ENS Identity (optional — for agent identity resolution)
# ENS_RPC_URL requires an Ethereum mainnet RPC to resolve ENS names.
# Use a free/public one like https://eth.llamarpc.com or get an API key
# from Alchemy/Infura/QuickNode for reliability.
AGENT_ENS_NAME=sentinel-agent.eth
ENS_RPC_URL=
ENS_REGISTRY_ADDRESS=

# Nitrolite State Channel (optional — for co-signed state channels)
# In local mode, start.sh auto-configures these with Anvil accounts.
NITROLITE_BROKER_URL=
NITROLITE_SIGNER_KEY=
NITROLITE_BROKER_ADDRESS=

# Uniswap v4 (optional — for real on-chain swap quotes)
# Set these to query Quoter2/PoolManager on Base Sepolia.
UNISWAP_V4_QUOTER_ADDRESS=
UNISWAP_V4_POOL_MANAGER_ADDRESS=

# API Server (backend port — frontend proxies here)
API_PORT=3001

# Session defaults
DEFAULT_SESSION_DEPOSIT_USDC=1000
MAX_TRADE_PERCENT=2
MAX_SLIPPAGE_PERCENT=0.5

# Logging
LOG_LEVEL=debug
